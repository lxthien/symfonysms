<div class="banner d-none hidden-xs hidden-sm">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div id="logo" class="text-center">
                    <a href="{{ path('homepage') }}" title="{{ get_setting('siteName') }}">
                        <img alt="{{ get_setting('siteName') }}" src="{{ asset('assets/images/logo.png') }}">
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>